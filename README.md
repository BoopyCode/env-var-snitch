# Environment Variable Snitch

Snitches on your misconfigured environment variables before they cause production fires. Run: `./env-snitch.sh` or `bash env-snitch.sh`. Use `-v VAR_NAME` to check additional variables and `-h` for help.